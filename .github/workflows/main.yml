name: Deployment

on:
  push:
    branches:
      - develop

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: develop
    steps:
      - name: deploy
        shell: bash
        run: |
          echo ${{ env.FOO }}
